# GPU passthrough override.
# Usage: PROJECT_NAME=myapp docker compose -f compose.yml -f compose.gpu.yml up -d
services:
  workspace:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
